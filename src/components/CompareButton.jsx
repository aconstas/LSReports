import { Button } from "@mui/material";

export default function CompareButton() {
  return (
    <Button variant="contained" type="submit" size="large" sx={{ mt: 6 }} color="warning">
      Compare
    </Button>
  );
}
